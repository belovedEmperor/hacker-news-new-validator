# How to Run
1. Install node modules by running `npm i`.
2. Install Playwright browsers with `npx playwright install chromium`.
3. Run `npm run start`

## On NixOS
1. Install node modules by running `npm i`.
2. Run `nix develop --command npm run start`
